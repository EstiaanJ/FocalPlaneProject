[package]
name = "focal-core"
version.workspace = true
edition.workspace = true
description = "CPU reference image-processing pipeline for FocalPlane"
license = "MIT"

[dependencies]
serde.workspace = true

[dev-dependencies]
serde_json.workspace = true

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "warn"
pedantic = "warn"
